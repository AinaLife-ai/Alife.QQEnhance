# Alife.QQEnhance

QQ增强插件：贴表情、点赞、撤回、禁言、戳一戳、引用回复、合并转发、音乐卡片、感知通知、输入中状态、戳回决策

从 KiraAI 的 [kira-ai-plugin-qq-enhance](https://github.com/xxynet/kira-ai-plugin-qq-enhance) 移植到 Alife 框架。

## 功能

- **贴表情**：`SetEmojiRecent`——双模式：默认对某人最近一条贴（免ID一步到位，index 可选倒数第N条）；也可直接传真实 messageId
- **资料卡点赞**：`SendQQLikes`，好友/陌生人均可，默认 50 次（顶满平台日上限），5×10 分块调用；成功回执可用配置开关（默认开）
- **撤回**：`DeleteMsgRecent`——三模式：默认撤 target 倒数第 index 条；传真实 messageId 直接撤；`list=true` 先出候选列表（序号+消息ID+折叠内容）再选撤。旧 `DeleteMsg` 已并入删除
- **禁言**：`GroupBan` 禁言/解除群成员
- **戳一戳**：`PokeGroupMember` 群聊 / `PokePrivateMember` 私聊 / `PokeBack` 被戳后回戳（冷却可配置，默认10秒防刷屏）
- **引用回复**：`ReplyRecent`——双模式：默认引用某人最近一条（targetId 可省略自动推断会话）；也可直接传真实 replyToId 引用指定消息。旧 `SendReplyMessage` 已并入删除
- **合并转发**：`ForwardRecent`（转发最近N条，免ID，缓存不足自动回拉历史补齐。混合节点：文本/图片/语音/视频/表情走完整CQ码内容节点原样重发；文件/嵌套转发/引用/卡片/音乐走真实消息ID节点结构完整保留；含 bot 自己发的消息且显示真实QQ昵称）+ `SendForwardById` + `SendForwardNew`（均支持群聊/私聊）
- **点歌/音乐卡片**：`SendMusicCard platform=search musicId=歌名`（也可 `platform=163/qq/kugou/migu/kuwo` 传平台原生ID原样透传）。样式由配置「音乐卡片样式」决定（默认 `custom`）：`custom` 自定义卡片 / `record` 语音条（需网易云歌曲ID与直链，只对 `search/163` 生效）；其它平台一律发原生卡片。被协议端**明确拒绝**时会按 163→custom→record 自动降级一次；超时（10秒未收到响应）不会重发，卡片可能已发出，请先用 QGetMessages 确认
- **感知通知**：被禁言/解禁、新成员进群、被戳（决策提示）、被赞资料卡（可回赞）、群消息被贴表情（自己消息被贴 / 他人消息被贴按开关分流，无法判定归属时不打扰）
- **消息ID查询**：`QGetMessages`（纯查询工具，含防抖：同会话15秒内超2次拒绝），实时捕获 + 历史回拉双来源，含 bot 自己发的消息
- **私聊输入中状态**

## 主动社交设计

插件通过四层机制让 AI 主动、自然地使用互动功能：

1. **场景触发式函数描述**：每个函数的描述写清"什么时候随手用"（如"看到有趣/赞同的消息随手贴个表情，不用说话直接贴"）
2. **一步函数**：SetEmojiRecent/ReplyRecent/DeleteMsgRecent/PokeBack 全部免ID一步完成，消灭"查ID→抄ID"的决策成本
3. **事件主动喂提示**：被戳/被赞/被贴表情时注入轻量行动建议（冷却可配置，默认10秒，可忽略）
4. **常驻社交风格提示**：OnAwake 注入一段可配置的行为总纲（配置项"社交风格提示词"，可自行改写或清空），函数文档仍走 Implicit 省 token

## 真实消息ID机制

QQ 消息 ID 通常是负数（部分协议端为 int32 哈希，同样可能为负），撤回/贴表情/引用/转发必须使用真实 ID。本插件有三层来源（**不依赖任何额外事件上报**，NapCat 无需开 message_sent/reportSelfMessage）：

1. **发送自存**：本插件发出的消息发送成功后自动记录真实 message_id
2. **历史回拉**：缓存不足时自动调 `get_group_msg_history`/`get_friend_msg_history` 补齐（返回的 message_id 真实可用，注意与分页参数 message_seq 区分）
3. **官方事件**：订阅 QChat 官方连接的事件流（notice 感知、被引用消息 ID 提取）

> 4.9.11 起已彻底移除"第二条 WS 捕获连接"，相关配置项（实时消息捕获/捕获连接地址/捕获连接Token）均为废弃项，保持默认即可，不需要再为插件额外开一个 WS 服务端。

## 与 YuYang.QQTools（幼央工具箱）兼容

| 兼容模式 | 行为 |
|---|---|
| `Auto`（默认） | 检测到幼央 → 重叠功能（戳一戳/引用回复/点赞/贴表情/撤回/输入中）自动让位 |
| `PreferQQEnhance` | 无视幼央，全部功能由本插件提供 |
| `PreferYuYang` | 重叠功能一律让位（幼央未装时自动回退自持） |
| `Off` | 不检测，全功能注册，手动管理 |

## 依赖

- `Alife.Function.FunctionCaller`（Xml函数执行器）
- `Alife.Function.QChat`（QQ聊天模块）

## 安装

将 `Alife.QQEnhance` 文件夹放入 Alife 的 `Plugins` 目录，同步环境后启用模块即可。

## 注意事项

- 需要先启用 Alife 官方 QQ聊天模块（`Alife.Function.QChat`）
- 本插件通过反射访问 `QChatService` 内部的 `OneBotClient`，不修改官方代码
- **QQ平台消息ID为负数**。操作指定消息必须使用 `QGetMessages` 返回的真实ID，严禁编造
- 引用回复使用结构化消息段 `{type:"reply",data:{id}}`（NapCat 原生支持；`send_msg` 没有顶层 reply 参数）
- **音乐卡片**：默认网易云官方卡片（music 消息段 type=163，KiraAI 同款逻辑），由 NapCat 本地渲染签名；"音乐卡片样式=custom"为免签名自定义音乐段（字段本地渲染）；样式=record 为直链语音条保底。若 163 卡片显示"发送者版本过低"，说明 NapCat 签名通道异常，可切换 custom/record
- 撤回合并转发卡片类消息会失败（NapCat 平台限制）
- 实时消息捕获参考了 [YuYang.QQTools](https://github.com/3026838203/YuYang.QQTools) 的 WebSocket 监听思路，特此致谢

## v4.9.28 更新说明

- **bot 样式自主权**：开了 ARK 之后，bot 仍可自行决定卡片形式——
  | 调用方式 | 结果 |
  |---|---|
  | `SendMusicCard musicId="晴天"`（不填 style） | 按配置（配了 ARK 则 **ARK 优先**） |
  | `SendMusicCard musicId="晴天" style="record"` | **语音条**（bot 明确要求，不受 ARK 影响） |
  | `SendMusicCard musicId="晴天" style="custom"` | **自定义卡片**（同上） |
  | `SendMusicCard musicId="晴天" style="163"` | 原生卡片（同上） |
  「音乐卡片」函数描述已同步说明，bot 知道可以自己选
- **非网易云平台（QQ音乐/酷狗等）也支持 custom/record**：此前被强制改回原生卡片，现在只要该平台能解析出音频直链就允许使用；拿不到直链时按原生卡片发送（bot 显式要求时会说明原因，配置默认导致的降级只记日志、不打扰 AI）
- **修复：非网易云平台走 custom/record 分支时歌曲信息为空** —— 此前"平台信息解析"只写在 163 分支内，走其它分支时从不解析，导致 ARK/签名服务拿到空字段被拒。现改为**无论走哪个分支都先统一解析一次**（网易云：歌名/歌手/封面/直链；其它平台：歌名/歌手/封面/跳转），字段复用、不重复请求

## v4.9.27 更新说明

- **修复 ARK 报「缺少必要参数」**：拼 URL 时若「ARK服务地址」里已带 query（如把 `?key=xxx` 一起填进去），会拼出**两个 `?`** → 服务端只认第一个，后面的 url/song/singer/cover/jump/format 全部丢失。现改为剥掉已有 query 再正确拼接，并**兼容"key 直接填在地址里"**的用法
- **修复 ARK 卡片显示不完整 / 只能试听（套壳补全）**：ARK 返回的卡片**缺 `extra{appid,uin}` 与 `meta.music` 的 `appid/app_type/ctime/uin/tagIcon`**，QQ 侧可能不按"官方应用卡片"渲染。现新增**套壳补全**：优先取签名服务产出的**完整卡作为模板**（复用其 appid/uin/tagIcon），把 ARK 的内容套进完整结构；无模板时用内置默认值 + bot 自身 uin
- **修复音频直链失效导致"只能试听"**：此前 `audio` 用的网易云 outer 外链**已实际失效**（实测 `302 → music.163.com/404`，返回 HTML 而非音频；上次仅凭 HTTP 200 误判为可用）。现改为**优先取真实直链**（meting → `music.126.net`）并做**可播校验**（跟随重定向后必须是 `audio/*` 且 >100KB），拿不到真直链才退回 outer
- **B站：降低 `-412` 风控** —— 先访问 `bilibili.com` 取**真实 `buvid3` cookie**（假 cookie 更易被拦），命中 `-412` 时**退避 1.5 秒自动重试一次**，并在日志中明确提示
- ARK 优先级保证：只要用户配置了 ARK（开启 + Token），**一定先用 ARK**

## v4.9.26 更新说明

- **修复：非网易云平台（QQ音乐/酷狗/酷我/咪咕）的卡片用不了 ARK 与签名** —— 两处原因：
  1. 这些平台**没有解析歌曲信息**，导致发给 ARK/签名服务的字段全空 → 被服务当缺参拒绝
  2. 签名走 `{type,id}` 形式，而实测 `ss.xingzhige.com` **只支持网易云**（`qq` 返回"关闭id解析功能"、`kugou/kuwo/migu` 直接 500）
  现改为：**非网易云平台也解析歌名/歌手/封面/跳转**（QQ音乐/酷狗走官方接口），**签名一律改用 custom 形式**（实测各平台内容都能签成功）
- **修复：ARK 一次失败就永久禁用** —— 此前任意失败都置 `_arkBroken`，于是"非网易云平台字段不全"这一次失败会**连累后续所有平台**（连网易云也跳过 ARK）。现按失败类型区分：
  - **服务不可用**（连不上/超时/非 JSON 应答）→ 永久标记，不再尝试
  - **业务失败**（服务正常应答但拒签，如字段不全）→ 只本次跳过，**下次仍会尝试**
- **修复：B站卡片"解析报错"** —— 原实现只看 `data` 是否存在，而 B站 失败时仍返回 HTTP 200（如 `code=-400 请求错误`，无 `data`），真实原因被吞掉；返回 HTML（风控）时 `JsonDocument.Parse` 抛异常 → 表现为"解析报错"。现改为：
  - 先判 `code`，按 B站 真实错误给出可读提示（-400 请求错误 / -404 视频不存在 / -352 触发风控）
  - JSON 解析单独容错，风控返回 HTML 时明确提示"可能被风控或限流，请稍后重试"并记录响应片段
  - **补全请求头**：`UA` + `Referer` + `Accept` + `Accept-Language` + `Cookie`（缺 Accept/Cookie 更易被风控）

## v4.9.25 更新说明

- **修复：开启第三方ARK后仍出现"版本过低"** —— ARK 此前只在「163 样式」路径内被检查，导致**自定义卡片（custom）、语音条（record）、非网易云平台**完全用不上 ARK，协议端签名不灵时照样显示版本过低。现 ARK 提升为**统一的最高优先级通道**，对所有平台、所有样式生效
- **修复：B站卡显示"版本过低"** —— 第三方签名服务不认识 B站数据，会按「QQ音乐图文卡」兜底产出（实测 `ss.xingzhige.com` 对 B站 返回 `app=com.tencent.tuwen.lua / appid=100497308 / tag=QQ音乐`，封面还被代理重挂），appid 与标签全错。现 **B站卡始终使用自建 news 卡**（B站官方分享卡 appid + `tag=哔哩哔哩` + 原始封面），**明确不走 ARK、也不走签名服务**（检测到已配置时会记一条日志说明原因）
- **修复：163 卡片被协议端拒绝后掉成语音条** —— 原因是兜底签名用 `{type:"163",id}` 形式，而签名服务对 **VIP/版权受限歌曲**查不到信息会直接失败（返回"无法准确获取歌曲信息"），于是连环降级到 record。现改为
  - **签名一律改用 custom 形式**：由插件提供歌名/歌手/封面/直链，签名服务无需再查歌，**VIP 歌也能签成功**（失败才兼容回退 `{type,id}` 形式）
  - **未探测时默认先试公共签名**（不再等协议端拒绝一次），成功即记下沿用（无感），失败才走协议端
  - 只有**签名服务本身不可用**（HTTP 失败）才标记不可用；"这首歌签不了"不标记，换歌仍会尝试
- 通道优先级（B站卡除外）：**第三方ARK（开启且有Token）→ 自建/公共签名服务 → 协议端原生 music 段（被拒时触发兜底）→ custom → record**

## v4.9.24 更新说明

- **新增「公共签名服务自动兜底」（默认开）**：协议端**明确拒绝**当前卡片时（典型：LLBot 未配置 `musicSignUrl` 直接报错「音乐卡片签名地址未配置」），自动改用公共签名服务重签为 json 段重发。
  - **成功即沿用**：一旦兜底成功，后续网易云卡片直接走签名通道（**无感**，不再先失败一次）
  - **失败即记下**：公共签名不可用只尝试一次，之后切回协议端通道，不会每次多等一次超时
  - 自己配了「音乐签名服务地址」的用户**不受影响**（尊重用户配置，不覆盖）；可用「签名服务自动兜底」开关关闭
- **新增「第三方ARK签名通道」（默认关，需 Token）**：开启并填入 Token 后，音乐卡片走第三方 ARK 签名服务（自建卡片、不依赖协议端签名）。配置项内已写明获取方式：访问 `https://apii.xianyuw.cn` 注册 → 复制 API Key → 填入「ARK服务Token」。「ARK服务地址」默认公共站、可换自建。签名失败一次即记下并切回默认通道（需重试可在配置中关闭再开启）
- 通道优先级：**第三方ARK（开启且有Token）→ 自建/公共签名服务 → 协议端原生 music 段（被拒时触发公共兜底）→ custom → record**

## v4.9.23 更新说明

- **提示冷却拆分为四个独立配置**（此前四类事件共用一个值，互相关联）：
  | 配置项 | 默认 | 说明 |
  |---|---|---|
  | 戳一戳提示冷却(秒) | 10 | 被戳的提示间隔 |
  | 被赞提示冷却(秒) | 10 | 资料卡被赞的提示间隔 |
  | 被贴表情提示冷却(秒) | 10 | 他人给「你的消息」贴表情 |
  | 他人贴他人表情提示冷却(秒) | **30** | 他人给「别人的消息」贴表情（较频繁，间隔放宽防刷屏） |
  旧配置项「提示冷却时间(秒)」标记为已废弃（保留兼容，不再生效）。冷却判定仍在**所有过滤之后**：自己贴的、取消回应、无法判定归属、被开关拦下的都不占冷却；**被跳过的事件不排队、不合并、直接丢弃**
- **新增 B站视频卡片**：`SendMusicCard platform=bilibili musicId=<BV号>`（也认 b站/哔哩哔哩/bili/BV 等别名），默认开启、可在配置中关闭。走 QQ 通用图文卡（`news`）由插件自行构造，**不依赖任何签名服务、也不受协议端平台白名单限制**；视频标题/UP主/封面自动从 B站公开接口获取
- **签名返回兜底补全 `prompt`/`ver`/`view`**：部分自建/第三方签名实现不返回这三个字段，QQ 端可能不渲染成卡片；改为缺失即补、已有值不覆盖，并对 news/music 卡自动推断 `view`
- **修正「音乐签名服务地址」描述**：此前写"该服务不稳定"不准确——实测 `ss.xingzhige.com` 稳定可用，失败仅发生在**网易云 VIP/版权受限歌曲**（返回"无法准确获取歌曲信息"），此时会自动降级为自定义卡片或语音条；并写明 NapCat 有内置默认签名服务（不填也能发）、LLBot 未配置 musicSignUrl 会直接报错（**必须填**）
- 卡片 JSON 统一使用宽松编码器（中文不转义），便于日志排查

## v4.9.22 更新说明

- **表情编号识别补全（识别用，不影响 AI 可见面）**：新增 QQ 系统表情**全量表**（329 条，来源 NapCat `face_config.json`，编号范围 0–484），仅用于识别——被动感知到"被贴表情"时能显示「编号（描述）」，如 `424（续标识）`、`66（爱心）`、`484（比爱心）×2`；此前只有 54 条精选表的编号才能显示描述，其余一律只显示数字。全表**不参与**任何提示词注入或主动查表输出，AI 可见面零增加
- **主动表（`emojiId=0` 查表）保持精选**（56 条），并修正两处历史笔误：`181` 骚扰 → **戳一戳**（与 QQ 全表一致）；`246=加油抱抱` 在 QQ 全表中**不存在**，替换为真实存在的 `311=打call`；另补入 `424=续标识`、`66=爱心`
- `SetEmojiRecent` 函数描述里引用的表情编号同步更新（移除失效的 246）

## v4.9.21 更新说明

- **点歌：audio 直链改用网易云官方 outer 外链**（`https://music.163.com/song/media/outer/url?id={id}.mp3`，302 到实际音频、协议端本地下载）。第三方解析直链（meting/vkeys）有防盗链/过期风险——表现为"卡片能出来但点开播不了"，故不再用于 audio 字段，只保留歌名/歌手/封面来源
- **降级顺序调整**：原生卡片被协议端明确拒绝时**优先降级 custom**，再降级语音条（部分协议端对 163 原生卡片依赖签名服务、直接抛错），顺序固定 163 → custom → record
- **platform 规范化（只做便利化，不改调用者意图）**：支持别名与大小写——`QQ音乐`→qq、`酷狗`→kugou、`酷我`→kuwo、`咪咕`→migu、`网易云/netease/wy`→网易云系；网易云系再按 `musicId` 形态细分（**纯数字视为歌曲ID直用，否则按关键词搜索**）；无法识别时按网易云处理，绝不把非法 type 丢给协议端
- **贴表情感知显示表情编号**：通知带出贴的是哪个表情——对照表内可解析时显示「编号（描述）」（如 `264（捂脸）`），未收录只显示编号；多个表情用 `、` 连接、最多 5 个、重复显示 `×N`。措辞给出可用参数但**不点名"贴回同一个"**（`或换个别的 emojiId`），由 AI 自行决定回应方式
- **SnowLuma 兼容**：① 兼容其 `sub_type: add/remove`——取消表情回应不再被当成新互动（不提示、不占冷却）；② 操作者名解析加 `get_stranger_info` 兜底、群成员信息失败不再抛错；③ 协议端未解析出操作者 UID（`user_id=0`）时降级为「某位用户」，不崩
- **修复 QGetMessages 不显示已撤回消息**：此前过滤掉了已撤回条目，导致表头宣称的"标注【已撤回】仅作存档"是死代码、AI 找不到自己刚撤的消息。现与撤回候选列表口径一致：已撤回条目保留内容并标注【已撤回】；误操作由各功能自身守卫拦下（会明确提示不能撤回/贴表情/引用）
- README 修正：移除"额外加一个 WS 捕获服务端"的过时说明（4.9.11 已移除第二条 WS，相关配置项均为废弃项）；冷却时间描述改为可配置（默认 10 秒）

## v4.9.20 更新说明

- **修复「点歌」在默认配置下原生平台不可用**：样式默认 `custom` 后，`platform=qq/kugou/migu/kuwo` 会被送进"需要网易云歌曲ID"的分支，把平台ID当成网易云关键词去搜，导致"未找到歌曲"。现在非网易云平台一律走原生卡片（ID 原样透传），custom/record 只对 `search/163` 生效
- **修复音乐卡片可能重复发送**：原"163 失败自动回退 custom"在**超时**时也会触发，而 10 秒超时只代表没收到响应（卡片可能已经发出）→ 可能发出两张。现在只有协议端**明确拒绝**（RetCode≠0）才降级；超时与语义不明的异常一律不重发，只提示先用 QGetMessages 确认
- **降级链与解析复用**：原生卡片被拒绝时按 163→custom→record 降级一次，全部复用同一次歌曲解析结果，降级路径不再重复搜索/重复请求；成功判定改为以协议端 retcode 为准（协议端不返回 message_id 时不再误报失败）
- **修复内置互动提示里的函数参数名**：默认提示里的 `{poke} target=` 与 `SendQQLikes target=` 参数名与实际签名（groupId/userId、qq）不符，AI 照抄会触发「缺少参数」。现改为新占位符 `{pokeargs}`（自动区分群聊/私聊）+ `qq=`；**老配置里的旧文本会在模块激活时自动修正并记日志**
- **贴表情感知分类化**：被贴消息归属分「自己 / 他人 / 无法判定」三类——自己贴的表情不再回灌提示；他人消息受「他人消息贴表情感知」开关约束；无法判定时只记一条调试日志、不打扰 AI。归属查询只走一轮（缓存 → get_msg 回查一次），失败不重试；提示冷却只在真正要提示时才占用
- 加固：官方 QChat 回复格式规则找不到时输出告警日志（便于排查"补标提醒"复现）；模块销毁时只移除本插件注册的那条规则，避免误删同名规则

## v4.9.19 更新说明

- **修复群聊引用/操作偶发错位（排序不一致）**：候选列表渲染按 Time→MessageId 排序，而实际定位按 Time→Seq（插入顺序）排序——QQ时间戳是秒级精度，同秒多条消息时两边序号会对不上，AI按列表报index却操作到另一条。现全插件统一为 Time→Seq（Seq继承NapCat历史页真实时序；不用MessageId比大小是因为QQ平台消息ID可能为负、大小关系不可靠），列表序号与实际解析在任何缓存状态下严格一致。影响面：撤回/贴表情/引用/转发/QGetMessages 全部排序点已对齐
- **QGetMessages 恢复已撤回存档展示**：之前查询过滤了已撤回消息，但表头宣称"标注【已撤回】仅作存档"，AI找不到刚撤的消息会困惑；现已撤回条目正常展示并标注（与候选列表一致）
- **已知ID直达补已撤回守卫**：SetEmojiRecent/ReplyRecent 直接传 messageId 时，若该消息已撤回会明确提示而不是发给NapCat碰运气
- **昵称跨会话匹配多人时不再猜会话**：targetId缺省+昵称匹配到多个不同QQ时，返回提示显式传 targetId，避免静默选错会话

## v4.9.18 更新说明

- **修复私聊"最近一条"定位到数天前旧消息（引用/撤回/列表错位）**：`get_friend_msg_history` 不传 message_seq 时实测可能返回陈旧页（最新消息是数天前），导致私聊引用回复引到旧消息。现私聊历史回拉改为两段式：常规拉取后，以缓存中该会话已知最大 message_id 为锚点**前向追新**（NapCat 传 message_seq 会返回该条之后更新的消息），逐页追上最新为止（最多4页防呆）。群聊路径不变。所有依赖私聊消息定位的功能（ReplyRecent 引用、DeleteMsgRecent 撤回、ForwardRecent 转发、QGetMessages/候选列表）同步修复
- 新增追新命中 Debug 日志（锚点/新增条数/页码），便于观测私聊历史新鲜度

## v4.9.17 更新说明

- **修复戳一戳无限回圈（重要）**：bot 回戳后，NapCat 会把"bot 戳了对方"也作为 poke 通知推回，旧代码只过滤"被戳对象"没过滤"发起者是自己"，导致把自己的回戳回执当成对方新戳、继续回戳、无限循环（私聊场景实测复现）。修复为三层防护：
  1. **发起者过滤**：poke 通知 sender==self（自己发起的戳，含回戳回执）一律无视——回圈从根上斩断
  2. **回执回声抑制**：主动戳人（含回戳）后 3 秒内收到同一人的戳通知视为我方动作的回执回声忽略（防 NapCat 私聊通知方向字段不规范时第一层兜不住；配置「回戳回执抑制秒数」，0=关闭）
  3. **防刷限次（保险丝）**：同一人在滑动窗口内最多受理 10 次戳一戳，超出静默忽略 AI 无感知（防双AI互戳/连戳脚本这类双方逻辑都正确的回圈；配置「戳一戳防刷限次」默认10 + 「戳一戳防刷窗口秒数」默认60）
- 新增 poke 通知原文诊断日志（Debug 级：user_id/target_id/self_id/group_id），再遇异常可直接定位来源

## v4.9.16 更新说明

- **修复歌曲搜索大面积失败（4.9.15 回归）**：4.9.15 把搜索收紧为网易云官方 web 搜索单源，但该接口裸调极不稳定（常返回空/-462），导致"晴天"等常见歌都搜不到。现改为三级静默兜底：**163api（NCM-Downloader 同款搜索源）最优先 → 网易云官方 web 搜索 → meting**，逐级自动切换、AI 全程无感知，只有三源全部失败才报"未找到歌曲"；未找到时的提示也改为建议换短关键词重试
- 搜索命中来源写入 Debug 日志，全失败写 Warning 日志

## v4.9.15 更新说明

- **音乐卡片完全复刻 KiraAI 版逻辑（修复"发送者版本过低"）**：
  - 平台ID原样透传零加工：`platform=163/qq/kugou/migu/kuwo` 时 musicId 不做任何处理直接进 music 消息段（此前存在多余加工路径）
  - 发送动作统一为 `send_msg`（与 KiraAI 逐字节一致），群聊/私聊由 `DetectIsGroupAsync` 自动判定，不再依赖调用方传对 messageType
  - 搜索源收紧为**网易云官方 web 搜索单一来源**（砍掉 163api/meting 可疑兜底源，避免拿到无效ID导致卡片渲染失败）
  - `json` 样式已废弃（依赖的公共签名服务已关停）：旧配置为 json 会自动按 163 发送并输出警告日志提示改配置
  - `custom` 免签名卡字段修正：补充 content+singer 双字段兼容（部分客户端只认其一），audio 缺失时自动补网易云 outer 外链兜底
  - 新增发送 payload 诊断日志（`音乐卡片发送 payload: ...`），出问题可直接比对 NapCat 日志定位

## v4.9.14 更新说明

- **修复私聊引用/撤回/贴表情/转发全部定位失败**：messageType 省略时旧代码一律按群聊处理，私聊里传对方QQ会被当成群号导致永远找不到消息。现改为会话类型自动判定（显式 messageType 优先 → GroupStates/缓存证据 → 群历史探测失败自动按私聊），ReplyRecent/SetEmojiRecent/DeleteMsgRecent/ForwardRecent/SendForwardById/SendForwardNew/QGetMessages 全部统一走该判定；QGetMessages 误把对方QQ传成群号时自动按私聊纠正。
- **封堵私聊贴表情假成功**：QQ 平台不支持私聊表情回应（接口返回成功但实际不生效），SetEmojiRecent 解析到私聊消息时如实提示"QQ私聊不支持贴表情回应，可用文字/戳一戳回应"。
- **私聊零参数引用**：判定为私聊时 ReplyRecent/SetEmojiRecent 省略 target 自动指向对方（私聊只有两个人）。

## v4.9.13 更新说明

- **删除撤回时间预拒**：115秒预判存在误判（实测1分钟内的消息被误报超时），且回执文案本已含超时可能性说明，故整个移除——任何消息都直接发撤回请求，只有 delete_msg 真实报错才提示。
- **列表快照机制**：list=true 出候选列表时拍下序号快照，之后 index=序号 严格按快照解析，**期间新消息进来不影响序号对应**；快照过期/换对象后自动回退实时解析。新配置项「列表快照有效期(秒)」默认10秒，0=禁用快照，范围0~600。
- **SetEmojiRecent / ReplyRecent 新增 list=true**：与撤回完全对称的候选列表能力，三个功能共用同一快照、同一序号体系；默认路径不变（不传list照样一步直贴/直引），不确定操作哪条时才用 list。
- **无快照时的实时解析跳过已撤回消息**：连续调用 DeleteMsgRecent 可像贴表情一样连撤最近的N条，无需列表无需ID。
- 候选列表同秒消息排序改用消息ID决胜，序号更稳定。

## v4.9.12 更新说明

- **移除撤回后台核验（重要纠错）**：4.9.11 的历史比对核验在原理上不成立——NapCat 的 get_group_msg_history 读的是本地消息数据库，delete_msg 撤回后本地记录不删除，目标消息永远"仍在历史中"，导致核验必然误报"撤回失败"并误导 AI（实测撤回成功却收到失败回执）。现改为：delete_msg 被接受即乐观标记【已撤回】存档并立即返回，成功默认静默；只有 delete_msg 直接报错才如实提示。「撤回核验延迟(秒)」配置已废弃保留兼容。
- **表情表内嵌查询**：SetEmojiRecent 传 emojiId=0 即可返回 QQ 官方表情 ID 完整对照表（50+个），看完重调一次即贴，不新增函数，不占常驻文档。
- **精简隐式激活文档**：首次调用本插件函数时框架自动注入的完整文档中，"详细说明"部分大幅精简（使用规则压缩为4条，超长表情ID表改为常用子集），推送体积显著减小。函数级文档由代码注解自动生成，不受影响。

## v4.9.11 更新说明
- **发布前全量审计修复**：修复通知事件 else-if 链断裂导致「成员进群感知」与「戳一戳回应决策」整体失效的严重 bug（group_ban 分支重复行所致）；撤回核验历史窗口由 20 条扩到 50 条，避免高流量群误判；修复 typing 指示器竞态（旧会话退出时误删新会话的取消令牌，导致无法停止）；已撤回消息被缓存修剪后重新回拉时正确恢复【已撤回】标注（此前存档会丢失）；同一消息并发重复撤回只起一个后台核验；戳一戳/贴表情提示补充群号参数避免跨群误判。



- **架构调整：彻底移除第二条 WS 捕获连接，不再依赖任何事件上报**。所有功能只建立在官方 OneBot 连接 + 历史消息 API 之上，NapCat 无需开启 message_sent/reportSelfMessage 等任何额外上报，也不用再配置捕获地址（相关配置项已废弃保留兼容）。
- **修复撤回核验假阴性（显示未撤回但实际已成功）**：4.9.9 的撤回确认依赖捕获 WS 上的撤回通知，通知不到就误报"很可能未撤回"。改为**历史比对核验**：撤回后1秒拉取历史比对（还在则1.5秒后复查一次），目标消息消失=确认撤回成功，仍在=明确失败并说明原因（超时/权限），不依赖事件推送，结果绝对可信。
- **撤回成功静默 + 后台核验**：撤回请求被接受后函数立即返回，核验在后台异步进行，成功默认静默不打扰，只有确认失败或核验异常才推送报错。新增配置项「撤回成功回执」（默认关，开启后成功也推送确认）与「撤回核验延迟(秒)」（默认1秒，首次核验消息仍在时自动再等1.5秒复查）。
- **超时消息撤前直拒**：自己的消息发送已超过约115秒时直接告知"超平台时限无法撤回"，不再发出必然失败的请求。
- **"引用让撤回"场景指引**：撤回候选列表始终为最新（操作前无条件刷新历史），AI 按引用里的内容摘要在列表中比对目标后用 index/messageId 撤，不额外占用常驻token。
- 被赞/被贴表情感知改走官方连接事件（原先依赖捕获WS的原始notice字段），配置含义不变。

## v4.9.10 更新说明

- **修复消息缓存不更新导致撤回/查询拿到陈旧消息**：bot 经官方 QChat 发出的消息只有在 NapCat 开启 message_sent 上报时才进实时捕获，此前缓存里可能只有几小时前的旧消息，撤回"最近一条"实际撤的是早已超时的旧消息（必失败），QGetMessages/候选列表也显示旧内容。现在撤回/贴表情/引用的目标定位、QGetMessages、ForwardRecent 在操作前**无条件刷新历史消息**（历史接口返回的 message_id 真实可撤回），不依赖捕获健康度，撤"自己最近一条"恢复必然成功。
- 转发前同样无条件刷新，保证转发的是此刻最新 N 条。

## v4.9.9 更新说明

- **撤回结果真实确认（修复"显示成功但没撤回"）**：NapCat 的 `delete_msg` 在某些失败场景（消息超约2分钟撤回时限、NapCat 内存中已丢失该消息）也会返回成功回包，插件此前据此直接播报"已撤回"。现在撤回请求发出后会等待事件流中的 `group_recall`/`friend_recall` 撤回通知做二次确认（5秒）：收到确认才报"已撤回"；未收到则明确提示"很可能并未真正撤回"及原因，不再谎报。捕获链路不可用时也会如实标注"无法二次确认"。
- **已撤回消息保留存档并标注【已撤回】**：收到撤回通知（无论谁发起）后，消息保留在缓存中作内容存档，QGetMessages 与撤回候选列表中标注【已撤回】；撤回/贴表情/引用的目标定位自动跳过已撤回消息，对已撤回消息再发起撤回会得到明确提示；候选列表序号与 index 参数严格一致（含已撤回项）。
- **修复合并转发富媒体丢失（4.9.8 实际未生效）**：4.9.8 的混合节点实现在发布前丢失，实际发出的转发里图片/语音/视频都退成了占位文字。本次恢复并完善：文本/图片/语音/视频/表情走内容节点（完整CQ码按原始URL重发真实媒体）；文件/嵌套转发/引用/卡片/音乐走真实ID节点（结构原样保留）；已撤回消息服务端取不到，自动降级为内容节点。
- 审计了同类"无异常即报成功"的调用：贴表情/点赞/戳一戳/禁言 NapCat 均会返回真实失败码，发送类接口以返回的 message_id 为凭，均无此问题；全部消息ID来源（实时捕获/发送回执/历史回拉）逐一核对均为 NapCat 真实ID，发送回执取的是 message_id 而非 res_id。

## v4.9.8 更新说明

- **函数合并（6→3）**：`DeleteMsg`/`SetEmoji`/`SendReplyMessage` 删除，能力全部并入 `DeleteMsgRecent`/`SetEmojiRecent`/`ReplyRecent` 三个双模式函数——默认 target+index 免ID一步到位，也可直接传真实 messageId/replyToId
- **撤回新增 list 模式**：`DeleteMsgRecent list=true` 列出目标最近10条候选（序号+真实消息ID+折叠内容，超15字符首5...尾5），AI 看完用序号或ID精确撤
- **QGetMessages 防抖**：同一会话15秒内查询超过2次拒绝，防AI递归查询
- **撤回一步到位**：targetId 缺省时自动推断最近会话，缓存miss自动回拉历史（bot自己经QChat发的消息也能撤）
- **合并转发显示真实昵称**：内容节点模式，bot自己的消息用 get_login_info 拉取的真实QQ昵称（含60秒节流重试），不再显示"我"
- **点赞成功回执**：新增配置开关（默认开）
- **感知开关独立**：被赞/被贴表情感知各自独立开关，默认关；提示冷却可配置（默认10秒）
- **表情ID对照表**：写入 Implicit 文档，零常驻token，AI取用时才注入
- **私聊输入中状态修复**：恢复被错误摘除的 ChatSent 订阅
- **成功静默**：发送类动作成功不再触发AI额外确认轮

## v4.9.1 修复说明

- **合并转发/贴表情 RetCode 1400 修复**：NapCat 对 `set_msg_emoji_like` 的 `emoji_id`、转发节点的 `id`/`uin`、reply 段的 `id` 均要求 **string** 类型，此前传数字导致 1400 参数错误，已全部修正
- **音乐卡片新增 record 语音条模式**（配置项 音乐卡片样式=record）：直接发网易云直链语音条，完全不依赖签名，任何端可播。卡片显示"发送者版本过低"说明 NapCat 未配置 `musicSignUrl`（WebUI → OneBot11 配置里填签名服务地址），不想配置就用 record 模式保底
- **新增互动提示**（官方消息过滤同款 ChatSend 钩子）：收到 QQ 消息时在末尾附加提示，提醒 AI 可随手贴表情/引用/戳一戳/点赞。配置项可开关、调概率（默认 100%）、自定义文本
